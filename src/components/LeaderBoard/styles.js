import styled from "@emotion/styled/macro";
import { motion } from "framer-motion";

export const StyledLeaderBoard = styled(motion.ul)`
  display: flex;
  flex-direction: column;
  row-gap: 10px;
`;
